agents:
  queue: "agent-runners-windows-amd64"

steps:
  # - label: ":bazel: Build And Test MSVC"
  #   commands:
  #     - bazelisk test //:streamTests

  - label: ":bazel: Build And Test Winlibs/clang"
    commands:
      - bazelisk build --config=winlibs-clang //:streamTests
  - label: ":bazel: Build And Test Winlibs/gcc"
    commands:
      - bazelisk build --config=winlibs-gcc //:streamTests
