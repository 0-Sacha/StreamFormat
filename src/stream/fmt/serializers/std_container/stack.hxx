#pragma once

#include <stack>
#include "stream/fmt/context/formatter_executor/formatter_type.hxx"

namespace stream::fmt {
    /*
        template <typename T, typename FormatterExecutor>
        struct FormatterType<std::stack<T>, FormatterExecutor>
        {
            [[nodiscard]] static inline std::expected<void, FMTResult> format(const std::stack<T>& t, FormatterExecutor& executor) {
            }
        };
    */
}
